<!DOCTYPE html>
<html>
  <head>
    <title>CheckList</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <script src="https://kit.fontawesome.com/959f0f6a88.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "CheckList", root_path, class: "navbar-brand" %>
        </div>
        <ul class="nav navbar-nav">
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
          <% if current_user.present? %>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <% else %> 
            <li><%= link_to "Log In", new_user_session_path %></li>
          <% end %>
        </ul>
        <% if user_signed_in? %>
          <%= link_to "New Item", new_item_path, class: "navbar-text navbar-link navbar-right" %>
        <% end %>
      </div>
    </nav> 
    <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
    <div class="container">
      <%= yield %>
    </div>
    <!-- this is where all the views get inserted to display to the user. any html markup outside of the yield tag is going to be common for all the views.   -->
  </body>
</html>
